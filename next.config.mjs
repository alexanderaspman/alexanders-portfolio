/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  
    images: {
      domains: ['media.crystallize.com'], // Add your domain here
    },
  }
  
  
  
export default nextConfig;
